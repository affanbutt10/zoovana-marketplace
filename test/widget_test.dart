// Basic smoke test to verify the app compiles without errors.
// The full app integration is covered by the spec test files.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Placeholder — app compiles', () {
    // All meaningful tests are in the spec test files under test/app/ and test/apis/.
    // This file exists to satisfy the default test runner entry point.
    expect(true, isTrue);
  });
}
